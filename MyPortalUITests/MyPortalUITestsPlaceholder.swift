import XCTest

/// Placeholder so the UI-test bundle has something to link. UI tests will live
/// here once the app has flows that are worth automating end-to-end (the OAuth
/// + school-config first-run flow currently makes launch tests flaky).
final class MyPortalUITestsPlaceholder: XCTestCase {}
